#include "PathFinderAstar.h"

Node *PathFinderAstar::FindPath(Graph graph, int startVertice, int endVertice) {
    for (int i = 0; i < 5000000; i++) {
        /* code */
    }
    return new Node();
}